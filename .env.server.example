# ── SERVIDOR ── en el servidor: copia a .env (queda SOLO ahí, nunca en git).
# Los puertos internos (PORT=4000, VITE_APP_PORT=3000) tienen que coincidir
# con lo que espera el Caddyfile para este grupo: grupo0_backend:4000 y
# grupo0_frontend:3000.

# ═══ Base de datos ═══
MYSQL_ROOT_PASSWORD=pon-una-clave-root-larga
MYSQL_DATABASE=test
MYSQL_USER=manager_test
MYSQL_PASSWORD=pon-una-clave-larga

# ═══ Backend ═══
PORT=4000
DB_HOST=grupo0_db
DB_PORT=3306
DB_USER=manager_test
DB_PASSWORD=pon-una-clave-larga
DB_NAME=test
ORIGIN=http://prueba.146.83.216.166.nip.io

# ═══ Frontend (Vite) ═══
VITE_APP_HOST=0.0.0.0
VITE_APP_PORT=3000
VITE_BACKEND_URL=http://prueba.146.83.216.166.nip.io
VITE_ALLOWED_HOSTS=prueba.146.83.216.166.nip.io
