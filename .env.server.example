# ── SERVIDOR ── en el servidor: copia a .env (queda SOLO ahí, nunca en git).
# Los puertos internos (PORT=4000, VITE_APP_PORT=3000) tienen que coincidir
# con lo que espera el Caddyfile para este grupo: grupo4_backend:4000 y
# grupo4_frontend:3000.

# ═══ Base de datos PostgreSQL ═══
POSTGRES_DB=test
POSTGRES_USER=manager_test
#POSTGRES_PASSWORD=pon-una-clave-larga

# ═══ Backend ═══
PORT=4004
DB_HOST=grupo4_db
DB_PORT=5432
DB_USER=manager_test
DB_PASSWORD=pon-una-clave-larga
DB_NAME=test
ORIGIN=http://grupo4.146.83.216.166.nip.io

# ═══ Frontend (Vite) ═══
VITE_APP_HOST=0.0.0.0
VITE_APP_PORT=3004
VITE_BACKEND_URL=http://grupo4.146.83.216.166.nip.io
VITE_ALLOWED_HOSTS=grupo4.146.83.216.166.nip.io